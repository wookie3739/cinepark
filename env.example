# 필수
POSTGRES_PASSWORD=

# 필수. HS256 (UTF-8 기준 32바이트 이상 권장)
JWT_SECRET=

# 선택
# JWT_ACCESS_EXPIRATION_MS=
# JWT_REFRESH_EXPIRATION_MS=

# SMTP (원하면 루트 .env — application-private.yml 이 있으면 거기서 넣어도 됨)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
